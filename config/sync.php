<?php return [
    // Конфигурация системы синхронизации.
    // Значения можно переопределить через переменные окружения в файле .env.
    // Список доверенных узлов (alias => url)
    'peers' => env('SYNC_PEERS', []),
    // Идентификатор текущего узла
    'self' => env('SYNC_SELF', 'node1'),
    // API‑ключ для подписи запросов
    'api_key' => env('SYNC_API_KEY', ''),
    // Флаг отладки
    'debug' => env('SYNC_DEBUG', false),
    // Количество дней для выгрузки событий (используется в исходящем режиме)
    'days_back' => env('SYNC_DAYS_BACK', 7),
    // Типы событий, которые нужно синхронизировать
    'event_types' => env('SYNC_EVENT_TYPES', []),
];